{
  "private": true,
  "name": "preact-test",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build --no-prerender",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "PREACT_APP_BACKEND_HOST=http://localhost:3010 preact watch --port 3011",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^7.17.0",
    "eslint-config-preact": "^1.1.3",
    "preact-cli": "^3.0.0",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "@ailabs-yating/asr-client-sdk-javascript": "^1.0.2",
    "axios": "^1.1.3",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2"
  }
}